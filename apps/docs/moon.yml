tasks:
  dev:
    command: "npm run dev -- --host=127.0.0.1 --port 4321"
    inputs: ["src/**/*"]
  build:
    command: "npm run build"
    inputs: ["src/**/*"]
    outputs: ["dist"]
  preview:
    command: "npm run preview"
